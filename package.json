{
  "name": "electric-blocks",
  "version": "0.0.1",
  "description": "",
  "main": "blocks.js",
  "scripts": {
    "build": "mkdir -p build && gcc -shared -fpic $(find blocks/*.c) -o build/blocks.so",
    "start": "node blocks.js",
    "test": "./node_modules/mocha/bin/mocha tests"
  },
  "author": "asm-jaime,caffeine",
  "license": "",
  "dependencies": {
    "ffi": "^2.2.0",
    "ref": "^1.3.5",
    "ref-struct": "^1.1.0"
  },
  "devDependencies": {
    "chai": "^4.1.1",
    "mocha": "^5.1.1",
    "node-gyp": "^3.6.2",
    "d3-vilog": "^0.0.8"
  }
}
